# Local dev (Mac hostname — only works when running on the Mac itself)
# EXPO_PUBLIC_API_BASE_URL=http://wunderkind-backend.lndo.site

# Local dev (physical device on LAN — run scripts/dev-proxy.py first, then use your Mac LAN IP)
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.156:8080

# Production
# EXPO_PUBLIC_API_BASE_URL=https://api.wunderkind.app
